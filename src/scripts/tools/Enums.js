import {__} from '@wordpress/i18n';
export default {
  "STATUS": {
    "DRAFT": "invoice-draft",
    "ISSUED": "invoice-issued",
    "SENT": "invoice-sent",
    "EXPIRED": "invoice-expired",
    "PAID": "invoice-paid",
    "CANCELLED": "invoice-cancelled",
    "LABELS": {
      "invoice-draft": __('Draft', 'app'),
      "invoice-issued": __('Issued', 'app'),
      "invoice-sent": __('Sent', 'app'),
      "invoice-expired": __('Expired', 'app'),
      "invoice-paid": __('Paid', 'app'),
      "invoice-cancelled": __('Cancelled', 'app'),
    },
    "COLORS": {
      "invoice-draft": __('Draft', 'app'),
      "invoice-issued": __('Issued', 'app'),
      "invoice-sent": __('Sent', 'app'),
      "invoice-expired": __('Expired', 'app'),
      "invoice-paid": __('Paid', 'app'),
      "invoice-cancelled": __('Cancelled', 'app'),
    }
  }
}
